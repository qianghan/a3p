{
  "name": "@naap/content-library",
  "version": "1.0.0",
  "description": "Generic gated content catalog — admin-curated assets (starting with YouTube videos), visible once a caller-defined entitlement check passes. Has no concept of tenants, roles, or specific features; deliberately reusable by any plugin or app in the monorepo.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@naap/database": "*"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
