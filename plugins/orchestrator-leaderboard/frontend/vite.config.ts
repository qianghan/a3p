import { createPluginConfig } from '@naap/plugin-build/vite';

export default createPluginConfig({
  name: 'orchestrator-leaderboard',
  displayName: 'Orchestrator Leaderboard',
  globalName: 'NaapPluginOrchestratorLeaderboard',
});
